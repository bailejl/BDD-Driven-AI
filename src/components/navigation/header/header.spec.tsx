// TODO need to figure out how to handle SVG files in React testing
describe('Header', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(
    //   <Router>
    //     <Header />
    //   </Router>
    // );
    // expect(baseElement).toBeTruthy();
  })
})
